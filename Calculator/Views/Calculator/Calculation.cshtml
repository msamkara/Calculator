
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />

    <title>CalculateMe</title>

    <style type="text/css">

        #p1 {
            position: relative;
            left: 230px;
            top: 10px;
            font-weight:bold;
            font-style:italic;
            font-size:22px;
            color:darkred;
            width:500px;
            height:100px;
            text-align:center;
            text-wrap:unset;
            
        }
 
        #DataEntryTable {
            position: absolute;
            left: 600px;
            top: 100px;
        }

        #TableResultBoxes {
            position: absolute;
            left: 100px;
            top: 250px;
            height: 150px;
            width: 400px;
            float: left;
            border-width:4px;
            border-color:crimson;
            background-color:#f2c3c3;
        }

        #TableResultBoxes00 {
            position: absolute;
            left: 100px;
            top: 100px;
            height: 100px;
            width: 400px;
            float: left;
            border-width: 3px;
            background-color:#92c3e6;   
            text-align:center   
        }


        .buttons {
            font-family: Tahoma;
            font-weight: bold;
            color: #E74C3C;
            text-align: center;
            height: 55px;
            width: 55px;
            font-size: 20px;
        }

        .td1a {
            height: 30px;
            width: 100px;
            text-align: center;
            font-family: Tahoma;
            text-decoration: solid;
            font-weight: bold;
            background-color: #215072;
            color: aliceblue;
            font-size: 22px;
        }

        .td1b {
            height: 30px;
            width: 100px;
            text-emphasis-color:brown;
            text-align: center;
            font-family: Tahoma;
            font-size:15px;
            font-weight: bold;
            background-color: #d8bac7;
            color:brown;
            
        }

        .td2 {
            height: 30px;
            width: 30px;
            font-family: Tahoma;
            font-weight: bold;
            border-color: white;
            text-align: center;
            color:#720f0f;
        } 



        .td3 {
              border-color:white;

              }

       
    </style>

</head>
<body>

    <p id="p1">js Calculator</p>

    <script>

        var result;
        var str;
        var dotindex;
        var dot;
        var i;
        var substr;
        var newstr;
        var afterdot;
        var dig;
        var n;
        var processor01;
        var processor02;
        var processor03;
        var mod;
        var j;
        var count;
  
        function button7() {

            

            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "7";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "7";

                /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                 str = document.getElementById("tdscreenbox").innerHTML;

             
              
                str = str.replace(",", "").replace(",", "").replace(",", "");
               
                dotindex = str.indexOf(".");
                

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/
    
            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "7";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "7";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "7";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "7";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/



            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "7";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "7";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/



            }

        }
        function button8() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "8";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "8";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "8";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "8";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "8";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "8";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "8";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "8";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

        }
        function button9() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "9";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "9";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "9";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "9";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "9";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "9";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "9";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "9";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }

        function buttonplus() {

            if (document.getElementById("tdoperationbox04").innerHTML == "" && document.getElementById("tdoperationbox02").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = "";
                document.getElementById("tdoperationbox04").innerHTML = "+";
            }
            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                if (document.getElementById("tdoperationbox04").innerHTML == "+") {

                    processor01 = document.getElementById("tdscreenbox").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox01").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) + parseFloat(processor02);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox01").innerHTML = result;

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox01").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }

                        
                        document.getElementById("tdoperationbox01").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox01").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdscreenbox").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "+";
                    document.getElementById("tdoperationbox05").innerHTML = "";
                    result = "";
                }
                else if (document.getElementById("tdoperationbox04").innerHTML == "-") {

                    processor01 = document.getElementById("tdscreenbox").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox01").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor02) - parseFloat(processor01);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox01").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox01").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox01").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdscreenbox").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "+";
                    document.getElementById("tdoperationbox05").innerHTML = "";
                    result = "";
                }


            }
            else if (document.getElementById("tdoperationbox03").innerHTML != "") {
                if (document.getElementById("tdoperationbox02").innerHTML == "*") {

                    processor01 = document.getElementById("tdoperationbox01").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox03").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) * parseFloat(processor02);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox01").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox01").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox01").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox03").innerHTML = "";
                    document.getElementById("tdoperationbox02").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "+";
                    document.getElementById("tdscreenbox").innerHTML = "";


                }
                else if (document.getElementById("tdoperationbox02").innerHTML == "/") {

                    processor01 = document.getElementById("tdoperationbox01").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox03").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) / parseFloat(processor02);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox01").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox01").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox01").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox03").innerHTML = "";
                    document.getElementById("tdoperationbox02").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "+";
                    document.getElementById("tdscreenbox").innerHTML = "";
                }
            }
            else if (document.getElementById("tdoperationbox07").innerHTML != "") {
                if (document.getElementById("tdoperationbox06").innerHTML == "*") {
                                       
                    if (document.getElementById("tdoperationbox04").innerHTML == "+") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor03 = document.getElementById("tdoperationbox07").innerHTML;
                        processor03 = processor03.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) + parseFloat(processor02) * parseFloat(processor03);
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox04").innerHTML = "+";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                        document.getElementById("tdoperationbox06").innerHTML = "";
                        document.getElementById("tdoperationbox07").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                    else if (document.getElementById("tdoperationbox04").innerHTML == "-") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor03 = document.getElementById("tdoperationbox07").innerHTML;
                        processor03 = processor03.replace(",", "").replace(",", "").replace(",", "").replace(",", "");


                        result = parseFloat(processor01) - (parseFloat(processor02) * parseFloat(processor03));
                        document.getElementById("tdoperationbox01").innerHTML = result;

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/


                        document.getElementById("tdoperationbox04").innerHTML = "+";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                        document.getElementById("tdoperationbox06").innerHTML = "";
                        document.getElementById("tdoperationbox07").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                }
                else if (document.getElementById("tdoperationbox06").innerHTML == "/") {
                  
                    if (document.getElementById("tdoperationbox04").innerHTML == "+") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor03 = document.getElementById("tdoperationbox07").innerHTML;
                        processor03 = processor03.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) + (parseFloat(processor02) / parseFloat(processor03));
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox04").innerHTML = "+";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                        document.getElementById("tdoperationbox06").innerHTML = "";
                        document.getElementById("tdoperationbox07").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                    else if (document.getElementById("tdoperationbox04").innerHTML == "-") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor03 = document.getElementById("tdoperationbox07").innerHTML;
                        processor03 = processor03.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) - (parseFloat(processor02) / parseFloat(processor03));
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox04").innerHTML = "+";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                        document.getElementById("tdoperationbox06").innerHTML = "";
                        document.getElementById("tdoperationbox07").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                }

            }

        }

        function button4() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "4";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "4";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "4";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "4";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "4";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "4";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "4";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "4";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }
        function button5() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "5";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "5";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "5";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "5";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "5";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "5";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "5";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "5";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }
        function button6() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "6";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "6";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "6";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "6";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "6";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "6";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "6";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "6";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }

        function buttonmin() {

            if (document.getElementById("tdoperationbox04").innerHTML == "" && document.getElementById("tdoperationbox02").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = "";
                document.getElementById("tdoperationbox04").innerHTML = "-";
            }
            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                if (document.getElementById("tdoperationbox04").innerHTML == "+") {

                    processor01 = document.getElementById("tdscreenbox").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox01").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) + parseFloat(processor02);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox01").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox01").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox01").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdscreenbox").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "-";
                    document.getElementById("tdoperationbox05").innerHTML = "";
                    result = "";
                }
                else if (document.getElementById("tdoperationbox04").innerHTML == "-") {

                    processor01 = document.getElementById("tdscreenbox").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox01").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor02) - parseFloat(processor01);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox01").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox01").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox01").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdscreenbox").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "-";
                    document.getElementById("tdoperationbox05").innerHTML = "";
                    result = "";
                }


            }
            else if (document.getElementById("tdoperationbox03").innerHTML != "") {
                if (document.getElementById("tdoperationbox02").innerHTML == "*") {

                    processor01 = document.getElementById("tdoperationbox01").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox03").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) * parseFloat(processor02);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox01").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox01").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox01").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox03").innerHTML = "";
                    document.getElementById("tdoperationbox02").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "-";
                    document.getElementById("tdscreenbox").innerHTML = "";


                }
                else if (document.getElementById("tdoperationbox02").innerHTML == "/") {

                    processor01 = document.getElementById("tdoperationbox01").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox03").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) / parseFloat(processor02);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox01").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox01").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox01").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/


                    document.getElementById("tdoperationbox03").innerHTML = "";
                    document.getElementById("tdoperationbox02").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "-";
                    document.getElementById("tdscreenbox").innerHTML = "";
                }
            }
            else if (document.getElementById("tdoperationbox07").innerHTML != "") {
                if (document.getElementById("tdoperationbox06").innerHTML == "*") {

                    processor01 = document.getElementById("tdoperationbox05").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox07").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor02) * parseFloat(processor01);
                    document.getElementById("tdoperationbox05").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox05").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox05").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox05").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox07").innerHTML = "";
                    document.getElementById("tdoperationbox06").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";

                    if (document.getElementById("tdoperationbox04").innerHTML == "+") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) + parseFloat(processor02);
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox04").innerHTML = "-";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                    }
                    else if (document.getElementById("tdoperationbox04").innerHTML == "-") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) - parseFloat(processor02);
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox04").innerHTML = "-";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                    }
                }
                else if (document.getElementById("tdoperationbox06").innerHTML == "/") {

                    processor01 = document.getElementById("tdoperationbox05").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox07").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) / parseFloat(processor02);
                    document.getElementById("tdoperationbox05").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox05").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox05").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox05").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox07").innerHTML = "";
                    document.getElementById("tdoperationbox06").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";

                    if (document.getElementById("tdoperationbox04").innerHTML == "+") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) + parseFloat(processor02);
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox04").innerHTML = "-";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                    }
                    else if (document.getElementById("tdoperationbox04").innerHTML == "-") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) - parseFloat(processor02);
                        document.getElementById("tdoperationbox01").innerHTML = result;
                        document.getElementById("tdoperationbox04").innerHTML = "-";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                    }
                }

            }
        }

        function button1() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "1";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "1";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "1";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "1";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "1";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "1";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "1";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "1";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }
        function button2() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "2";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "2";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "2";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "2";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "2";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "2";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "2";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "2";

  /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }
        function button3() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "3";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "3";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "3";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "3";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "3";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "3";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/
            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "3";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "3";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }


        function buttondiv() {

            if (document.getElementById("tdoperationbox05").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdoperationbox06").innerHTML = "/";
                document.getElementById("tdscreenbox").innerHTML = "";
            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {
                if (document.getElementById("tdoperationbox06").innerHTML == "/") {

                    processor01 = document.getElementById("tdscreenbox").innerHTML;
                    processor01 = processor01.replace(",", "");
                    processor02 = document.getElementById("tdoperationbox05").innerHTML;
                    processor02 = processor02.replace(",", "");

                    result = parseFloat(processor02) / parseFloat(processor01);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox05").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox05").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox05").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox05").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox06").innerHTML = "/";
                    document.getElementById("tdoperationbox07").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";

                }
                else if (document.getElementById("tdoperationbox06").innerHTML == "*") {

                    processor01 = document.getElementById("tdscreenbox").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox05").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor02) * parseFloat(processor01);
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdoperationbox05").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox05").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox05").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox05").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox06").innerHTML = "/";
                    document.getElementById("tdoperationbox07").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";

                }

            }
            else if (document.getElementById("tdoperationbox04").innerHTML == "") {
                if (document.getElementById("tdoperationbox02").innerHTML == "") {

                    document.getElementById("tdoperationbox02").innerHTML = "/";
                    document.getElementById("tdscreenbox").innerHTML = "";
                }
                else if (document.getElementById("tdoperationbox02").innerHTML != "") {
                    if (document.getElementById("tdoperationbox02").innerHTML == "/") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox03").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) / parseFloat(processor02);
                        result = Math.round(result * 100) / 100;
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox02").innerHTML = "/";
                        document.getElementById("tdoperationbox03").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                    else if (document.getElementById("tdoperationbox02").innerHTML == "*") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox03").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                        result = parseFloat(processor01) * parseFloat(processor02);
                        result = Math.round(result * 100) / 100;
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox02").innerHTML = "/";
                        document.getElementById("tdoperationbox03").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }

                }

            }

        }

        function button0() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "0";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + "0";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "0";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + "0";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "0";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + "0";

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + "0";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + "0";

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }

        function buttonprod() {

            if (document.getElementById("tdoperationbox05").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdoperationbox06").innerHTML = "*";
                document.getElementById("tdscreenbox").innerHTML = "";
            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {
                if (document.getElementById("tdoperationbox06").innerHTML == "/") {

                    processor01 = document.getElementById("tdscreenbox").innerHTML;
                    processor01 = processor01.replace(",", "");
                    processor02 = document.getElementById("tdoperationbox05").innerHTML;
                    processor02 = processor02.replace(",", "");

                    result = parseFloat(processor02) / parseFloat(processor01);
                    document.getElementById("tdoperationbox05").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox05").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox05").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox05").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox06").innerHTML = "*";
                    document.getElementById("tdoperationbox07").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";

                }
                else if (document.getElementById("tdoperationbox06").innerHTML == "*") {

                    processor01 = document.getElementById("tdscreenbox").innerHTML;
                    processor01 = processor01.replace(",", "");
                    processor02 = document.getElementById("tdoperationbox05").innerHTML;
                    processor02 = processor02.replace(",", "");

                    result = parseFloat(processor02) *
                        parseFloat(processor01);
                    document.getElementById("tdoperationbox05").innerHTML = result;

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdoperationbox05").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdoperationbox05").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdoperationbox05").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox06").innerHTML = "*";
                    document.getElementById("tdoperationbox07").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";

                }

            }
            else if (document.getElementById("tdoperationbox04").innerHTML == "") {
                if (document.getElementById("tdoperationbox02").innerHTML == "") {

                    document.getElementById("tdoperationbox02").innerHTML = "*";
                    document.getElementById("tdscreenbox").innerHTML = "";
                }
                else if (document.getElementById("tdoperationbox02").innerHTML != "") {
                    if (document.getElementById("tdoperationbox02").innerHTML == "/") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "");
                        processor02 = document.getElementById("tdoperationbox03").innerHTML;
                        processor02 = processor02.replace(",", "");

                        result = parseFloat(processor01) / parseFloat(processor02);
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox02").innerHTML = "*";
                        document.getElementById("tdoperationbox03").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                    else if (document.getElementById("tdoperationbox02").innerHTML == "*") {

                        processor01 = document.getElementById("tdoperationbox01").innerHTML;
                        processor01 = processor01.replace(",", "");
                        processor02 = document.getElementById("tdoperationbox03").innerHTML;
                        processor02 = processor02.replace(",", "");

                        result = parseFloat(processor01) * parseFloat(processor02)
                        document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdoperationbox01").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdoperationbox01").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdoperationbox01").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox02").innerHTML = "*";
                        document.getElementById("tdoperationbox03").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }

                }

            }

        }

        function buttonC() {
            document.getElementById("tdscreenbox").innerHTML = "";
            document.getElementById("tdresultbox").innerHTML = "";
            document.getElementById("tdoperationbox01").innerHTML = "";
            document.getElementById("tdoperationbox02").innerHTML = "";
            document.getElementById("tdoperationbox03").innerHTML = "";
            document.getElementById("tdoperationbox04").innerHTML = "";
            document.getElementById("tdoperationbox05").innerHTML = "";
            document.getElementById("tdoperationbox06").innerHTML = "";
            document.getElementById("tdoperationbox07").innerHTML = "";


        }

        function buttonEQ() {

            if (document.getElementById("tdoperationbox07").innerHTML != "") {
                if (document.getElementById("tdoperationbox06").innerHTML == "*") {
                    if (document.getElementById("tdoperationbox04").innerHTML == "+") {

                        processor01 = document.getElementById("tdoperationbox07").innerHTML;
                        processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        processor03 = document.getElementById("tdoperationbox01").innerHTML;
                        processor03 = processor03.replace(",", "").replace(",", "").replace(",", "").replace(",", "");


                        result = parseFloat(processor01) * parseFloat(processor02) + parseFloat(processor03);
                        result = Math.round(result * 100) / 100;
                        document.getElementById("tdresultbox").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdresultbox").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdresultbox").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdresultbox").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox07").innerHTML = "";
                        document.getElementById("tdoperationbox06").innerHTML = "";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                        document.getElementById("tdoperationbox04").innerHTML = "";
                        document.getElementById("tdoperationbox03").innerHTML = "";
                        document.getElementById("tdoperationbox02").innerHTML = "";
                        document.getElementById("tdoperationbox01").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                    else if (document.getElementById("tdoperationbox04").innerHTML == "-") {

                        processor01 = document.getElementById("tdoperationbox07").innerHTML;
                        processor01 = processor01.replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "");
                        processor03 = document.getElementById("tdoperationbox01").innerHTML;
                        processor03 = processor03.replace(",", "");

                        result = parseFloat(processor03) - parseFloat(processor01) * parseFloat(processor02);
                            
                        result = Math.round(result * 100) / 100;
                        document.getElementById("tdresultbox").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdresultbox").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdresultbox").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdresultbox").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox07").innerHTML = "";
                        document.getElementById("tdoperationbox06").innerHTML = "";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                        document.getElementById("tdoperationbox04").innerHTML = "";
                        document.getElementById("tdoperationbox03").innerHTML = "";
                        document.getElementById("tdoperationbox02").innerHTML = "";
                        document.getElementById("tdoperationbox01").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                }
                else if (document.getElementById("tdoperationbox06").innerHTML == "/") {
                    if (document.getElementById("tdoperationbox04").innerHTML == "+") {

                        processor01 = document.getElementById("tdoperationbox07").innerHTML;
                        processor01 = processor01.replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "");
                        processor03 = document.getElementById("tdoperationbox01").innerHTML;
                        processor03 = processor03.replace(",", "");

                        result = parseFloat(processor03) + (parseFloat(processor02) / parseFloat(processor01)) ;
                            
                        result = Math.round(result * 100) / 100;
                        document.getElementById("tdresultbox").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdresultbox").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdresultbox").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdresultbox").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox07").innerHTML = "";
                        document.getElementById("tdoperationbox06").innerHTML = "";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                        document.getElementById("tdoperationbox04").innerHTML = "";
                        document.getElementById("tdoperationbox03").innerHTML = "";
                        document.getElementById("tdoperationbox02").innerHTML = "";
                        document.getElementById("tdoperationbox01").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                    else if (document.getElementById("tdoperationbox04").innerHTML == "-") {

                        processor01 = document.getElementById("tdoperationbox07").innerHTML;
                        processor01 = processor01.replace(",", "");
                        processor02 = document.getElementById("tdoperationbox05").innerHTML;
                        processor02 = processor02.replace(",", "");
                        processor03 = document.getElementById("tdoperationbox01").innerHTML;
                        processor03 = processor03.replace(",", "");

                        result = parseFloat(processor03) - parseFloat(processor02) / parseFloat(processor01);
                            
                        result = Math.round(result * 100) / 100;
                        document.getElementById("tdresultbox").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        newstr = "";

                        str = document.getElementById("tdresultbox").innerHTML;

                        str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                        dotindex = str.indexOf(".");

                        if (dotindex == -1) {

                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - i, str.length - i + 3);
                                newstr = substr + "," + newstr;

                            }

                            n = str.length % 3;
                            if (n == 0) {

                                newstr = newstr.substring(0, newstr.length - 1);

                            }
                            else {
                                newstr = str.substring(0, n) + "," + newstr;
                                newstr = newstr.substring(0, newstr.length - 1);
                            }


                            document.getElementById("tdresultbox").innerHTML = newstr;

                        }

                        else {


                            afterdot = str.substring(dotindex, str.length + 1);

                            dig = str.length - dotindex;
                            for (i = 3; i <= str.length; i += 3) {

                                substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                                newstr = substr + "," + newstr;
                            }
                            if (newstr.substring(0, 1) == ",") {
                                newstr = newstr.substring(1, newstr.length);

                            }

                            newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                            document.getElementById("tdresultbox").innerHTML = newstr;
                        }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                        document.getElementById("tdoperationbox07").innerHTML = "";
                        document.getElementById("tdoperationbox06").innerHTML = "";
                        document.getElementById("tdoperationbox05").innerHTML = "";
                        document.getElementById("tdoperationbox04").innerHTML = "";
                        document.getElementById("tdoperationbox03").innerHTML = "";
                        document.getElementById("tdoperationbox02").innerHTML = "";
                        document.getElementById("tdoperationbox01").innerHTML = "";
                        document.getElementById("tdscreenbox").innerHTML = "";
                    }
                }
            }
            else if (document.getElementById("tdoperationbox05").innerHTML != "" && document.getElementById("tdoperationbox07").innerHTML == "") {
                if (document.getElementById("tdoperationbox04").innerHTML = "+") {

                    processor01 = document.getElementById("tdoperationbox01").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox05").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) + parseFloat(processor02);
                        
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdresultbox").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdresultbox").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdresultbox").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdresultbox").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox06").innerHTML = "";
                    document.getElementById("tdoperationbox05").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "";
                    document.getElementById("tdoperationbox03").innerHTML = "";
                    document.getElementById("tdoperationbox02").innerHTML = "";
                    document.getElementById("tdoperationbox01").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";

                }
                else if (document.getElementById("tdoperationbox04").innerHTML = "-") {

                    processor01 = document.getElementById("tdoperationbox01").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox05").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) - parseFloat(processor02);
                        
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdresultbox").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdresultbox").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdresultbox").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdresultbox").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox06").innerHTML = "";
                    document.getElementById("tdoperationbox05").innerHTML = "";
                    document.getElementById("tdoperationbox04").innerHTML = "";
                    document.getElementById("tdoperationbox03").innerHTML = "";
                    document.getElementById("tdoperationbox02").innerHTML = "";
                    document.getElementById("tdoperationbox01").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";

                }
            }
            else if (document.getElementById("tdoperationbox03").innerHTML != "") {
                if (document.getElementById("tdoperationbox02").innerHTML == "/") {

                    processor01 = document.getElementById("tdoperationbox01").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox03").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) / parseFloat(processor03);
                        
                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdresultbox").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdresultbox").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdresultbox").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdresultbox").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox03").innerHTML = "";
                    document.getElementById("tdoperationbox02").innerHTML = "";
                    document.getElementById("tdoperationbox01").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";
                }
                else if (document.getElementById("tdoperationbox02").innerHTML == "*") {

                    processor01 = document.getElementById("tdoperationbox01").innerHTML;
                    processor01 = processor01.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    processor02 = document.getElementById("tdoperationbox03").innerHTML;
                    processor02 = processor02.replace(",", "").replace(",", "").replace(",", "").replace(",", "");

                    result = parseFloat(processor01) * parseFloat(processor02);

                    result = Math.round(result * 100) / 100;
                    document.getElementById("tdresultbox").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    newstr = "";

                    str = document.getElementById("tdresultbox").innerHTML;

                    str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                    dotindex = str.indexOf(".");

                    if (dotindex == -1) {

                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - i, str.length - i + 3);
                            newstr = substr + "," + newstr;

                        }

                        n = str.length % 3;
                        if (n == 0) {

                            newstr = newstr.substring(0, newstr.length - 1);

                        }
                        else {
                            newstr = str.substring(0, n) + "," + newstr;
                            newstr = newstr.substring(0, newstr.length - 1);
                        }


                        document.getElementById("tdresultbox").innerHTML = newstr;

                    }

                    else {


                        afterdot = str.substring(dotindex, str.length + 1);

                        dig = str.length - dotindex;
                        for (i = 3; i <= str.length; i += 3) {

                            substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                            newstr = substr + "," + newstr;
                        }
                        if (newstr.substring(0, 1) == ",") {
                            newstr = newstr.substring(1, newstr.length);

                        }

                        newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                        document.getElementById("tdresultbox").innerHTML = newstr;
                    }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                    document.getElementById("tdoperationbox03").innerHTML = "";
                    document.getElementById("tdoperationbox02").innerHTML = "";
                    document.getElementById("tdoperationbox01").innerHTML = "";
                    document.getElementById("tdscreenbox").innerHTML = "";
                }
            }
            else if (document.getElementById("tdoperationbox01").innerHTML != "" &&
                document.getElementById("tdoperationbox02").innerHTML == "" &&
                document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdresultbox").innerHTML = document.getElementById("tdoperationbox01").innerHTML;

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdresultbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdresultbox").innerHTML = newstr;
                    
                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdresultbox").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                document.getElementById("tdscreenbox").innerHTML = "";
                document.getElementById("tdoperationbox01").innerHTML = "";
            }
        }

        function buttondot() {
            if (document.getElementById("tdoperationbox02").innerHTML == "" && document.getElementById("tdoperationbox04").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + ".";
                document.getElementById("tdoperationbox01").innerHTML = document.getElementById("tdoperationbox01").innerHTML + ".";

            }

            else if (document.getElementById("tdoperationbox04").innerHTML != "" && document.getElementById("tdoperationbox06").innerHTML == "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + ".";
                document.getElementById("tdoperationbox05").innerHTML = document.getElementById("tdoperationbox05").innerHTML + ".";

            }
            else if (document.getElementById("tdoperationbox06").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + ".";
                document.getElementById("tdoperationbox07").innerHTML = document.getElementById("tdoperationbox07").innerHTML + ".";

            }
            else if (document.getElementById("tdoperationbox02").innerHTML != "") {

                document.getElementById("tdscreenbox").innerHTML = document.getElementById("tdscreenbox").innerHTML + ".";
                document.getElementById("tdoperationbox03").innerHTML = document.getElementById("tdoperationbox03").innerHTML + ".";

            }


        }

        function buttonxsq() {

            if (document.getElementById("tdoperationbox07").innerHTML != "") {
                result = parseFloat(document.getElementById("tdscreenbox").innerHTML) * parseFloat(document.getElementById("tdscreenbox").innerHTML);
                result = Math.round(result * 100) / 100;
                document.getElementById("tdscreenbox").innerHTML = result;
                document.getElementById("tdoperationbox07").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/


            }
            else if (document.getElementById("tdoperationbox05").innerHTML != "" && document.getElementById("tdoperationbox07").innerHTML == "") {
                result = parseFloat(document.getElementById("tdoperationbox05").innerHTML) * parseFloat(document.getElementById("tdoperationbox05").innerHTML);
                result = Math.round(result * 100) / 100;
                document.getElementById("tdscreenbox").innerHTML = result;
                document.getElementById("tdoperationbox05").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox03").innerHTML != "") {
                result = parseFloat(document.getElementById("tdoperationbox03").innerHTML) * parseFloat(document.getElementById("tdoperationbox03").innerHTML);
                result = Math.round(result * 100) / 100;
                document.getElementById("tdscreenbox").innerHTML = result;
                document.getElementById("tdoperationbox03").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox01").innerHTML != "" &&
                document.getElementById("tdoperationbox03").innerHTML == "" &&
                document.getElementById("tdoperationbox05").innerHTML == "") {
                result = parseFloat(document.getElementById("tdoperationbox01").innerHTML) * parseFloat(document.getElementById("tdoperationbox01").innerHTML);
                result = Math.round(result * 100) / 100;
                document.getElementById("tdscreenbox").innerHTML = result;
                document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
        }

        function buttonsqr() {

            if (document.getElementById("tdoperationbox07").innerHTML != "") {
                result = Math.sqrt(document.getElementById("tdoperationbox07").innerHTML);
                result = Math.round(result * 100) / 100;
                document.getElementById("tdscreenbox").innerHTML = result;
                document.getElementById("tdoperationbox07").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
            else if (document.getElementById("tdoperationbox05").innerHTML != "" && document.getElementById("tdoperationbox07").innerHTML == "") {
                result = Math.sqrt(document.getElementById("tdoperationbox05").innerHTML);
                result = Math.round(result * 100) / 100;
                document.getElementById("tdscreenbox").innerHTML = result;
                document.getElementById("tdoperationbox05").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/


            }
            else if (document.getElementById("tdoperationbox03").innerHTML != "") {
                result = Math.sqrt(document.getElementById("tdoperationbox03").innerHTML);
                result = Math.round(result * 100) / 100;
                document.getElementById("tdscreenbox").innerHTML = result;
                document.getElementById("tdoperationbox03").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

            else if (document.getElementById("tdoperationbox01").innerHTML != "" &&
                document.getElementById("tdoperationbox03").innerHTML == "" &&
                document.getElementById("tdoperationbox05").innerHTML == "") {
                result = Math.sqrt(document.getElementById("tdoperationbox01").innerHTML);
                result = Math.round(result * 100) / 100;
                document.getElementById("tdscreenbox").innerHTML = result;
                document.getElementById("tdoperationbox01").innerHTML = result;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdscreenbox").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }

        }

        function buttondel() {

            if (document.getElementById("tdoperationbox07").innerHTML != "") {

                newstr = document.getElementById("tdoperationbox07").innerHTML;
                newstr = newstr.substring(0, newstr.length - 1);
                document.getElementById("tdoperationbox07").innerHTML = newstr;
                document.getElementById("tdscreenbox").innerHTML = newstr;

                /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdoperationbox07").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox07").innerHTML = newstr;
                }

                /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/
            }

            else if (document.getElementById("tdoperationbox07").innerHTML == "" && document.getElementById("tdoperationbox05").innerHTML != "") {

                newstr = document.getElementById("tdoperationbox05").innerHTML;
                newstr = newstr.substring(0, newstr.length - 1);
                document.getElementById("tdoperationbox05").innerHTML = newstr;
                document.getElementById("tdscreenbox").innerHTML = newstr;

                /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdoperationbox05").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox05").innerHTML = newstr;
                }

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/
            }

            else if (document.getElementById("tdoperationbox03").innerHTML != "") {

                newstr = document.getElementById("tdoperationbox03").innerHTML;
                newstr = newstr.substring(0, newstr.length - 1);
                document.getElementById("tdoperationbox03").innerHTML = newstr;
                document.getElementById("tdscreenbox").innerHTML = newstr;

 /*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdoperationbox03").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox03").innerHTML = newstr;
                }

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/
            }

            else if (document.getElementById("tdoperationbox01").innerHTML != "" && document.getElementById("tdoperationbox03").innerHTML == "" && document.getElementById("tdoperationbox05").innerHTML == "") {

                newstr = document.getElementById("tdoperationbox01").innerHTML;
                newstr = newstr.substring(0, newstr.length - 1);
                document.getElementById("tdoperationbox01").innerHTML = newstr;
                document.getElementById("tdscreenbox").innerHTML = newstr;

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

                newstr = "";

                str = document.getElementById("tdoperationbox01").innerHTML;

                str = str.replace(",", "").replace(",", "").replace(",", "");
                dotindex = str.indexOf(".");

                if (dotindex == -1) {

                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - i, str.length - i + 3);
                        newstr = substr + "," + newstr;

                    }

                    n = str.length % 3;
                    if (n == 0) {

                        newstr = newstr.substring(0, newstr.length - 1);

                    }
                    else {
                        newstr = str.substring(0, n) + "," + newstr;
                        newstr = newstr.substring(0, newstr.length - 1);
                    }


                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;

                }

                else {


                    afterdot = str.substring(dotindex, str.length + 1);

                    dig = str.length - dotindex;
                    for (i = 3; i <= str.length; i += 3) {

                        substr = str.substring(str.length - dig - i, str.length - dig - i + 3);
                        newstr = substr + "," + newstr;
                    }
                    if (newstr.substring(0, 1) == ",") {
                        newstr = newstr.substring(1, newstr.length);

                    }

                    newstr = newstr.substring(0, newstr.length - 1) + afterdot;

                    document.getElementById("tdscreenbox").innerHTML = newstr;
                    document.getElementById("tdoperationbox01").innerHTML = newstr;
                }

/*thousands seperator>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>*/

            }
 
        }

    </script>

    <table id="DataEntryTable" border="1" cellpadding="10" cellspacing="3">
        <tr>
            <td id="tdentry">
                <input type="button" class="buttons" id="button7" value="7" onclick="button7()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="button8" value="8" onclick="button8()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="button9" value="9" onclick="button9()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttonplus" value="+" onclick="buttonplus()">
            </td>
        </tr>

        <tr>
            <td id="tdentry">
                <input type="button" class="buttons" id="button4" value="4" onclick="button4()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="button5" value="5" onclick="button5()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="button6" value="6" onclick="button6()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttonmin" value="-" onclick="buttonmin()">
            </td>
        </tr>

        <tr>
            <td id="tdentry">
                <input type="button" class="buttons" id="button1" value="1" onclick="button1()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="button2" value="2" onclick="button2()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="button3" value="3" onclick="button3()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttondiv" value="/" onclick="buttondiv()">
            </td>
        </tr>

        <tr>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttonDOT" value="." onclick="buttondot()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="button0" value="0" onclick="button0()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttondel" value="DEL" onclick="buttondel()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttonproduct" value="*" onclick="buttonprod()">
            </td>
        </tr>

        <tr>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttonxsq" value="X2" onclick="buttonxsq()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttonrootsq" value="RSQ" onclick="buttonsqr()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttonC" value="C" onclick="buttonC()">
            </td>
            <td id="tdentry">
                <input type="button" class="buttons" id="buttonEQ" value="=" onclick="buttonEQ()">
            </td>

        </tr>

    </table>

    <table id="TableResultBoxes00" border="1" class="tables" cellpadding="10" cellspacing="10">
        <tr class="tr1">
            <td id="tdresultbox" class="td1a"></td>
        </tr>

        <tr class="tr1">
            <td id="tdscreenbox" class="td1a" align="right"></td>
        </tr>
    </table>

      <table id="TableResultBoxes" border="1" class="tables" cellpadding="10" cellspacing="10">

                <tr>
                    <td id="tdoperationbox01" class="td1b"></td>
                    <td id="tdoperationbox02" class="td2"></td>
                    <td id="tdoperationbox03" class="td1b"></td>
                </tr>

                <tr>
                    <td id="tdoperationbox04" class="td2"></td>
                </tr>

                <tr>
                    <td id="tdoperationbox05" class="td1b"></td>
                    <td id="tdoperationbox06" class="td2"></td>
                    <td id="tdoperationbox07" class="td1b"></td>
                </tr>
       </table>

</body>

</html>
